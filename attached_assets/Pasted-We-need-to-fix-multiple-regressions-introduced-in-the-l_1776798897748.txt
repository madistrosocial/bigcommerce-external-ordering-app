We need to fix multiple regressions introduced in the latest update related to BigCommerce search, inventory push, and max purchase quantity detection.

DO NOT change any working logic outside of what is listed.

----------------------------------------
1. FIX 403 ERROR (CRITICAL)
----------------------------------------

Problem:
The frontend is calling:
GET /api/bigcommerce/products/search

This route is protected by requireAdmin, causing 403 errors for POS users.

Fix:
- Replace ALL frontend calls to:
  /api/bigcommerce/products/search

  WITH:
  /api/agent/bigcommerce/search

- Ensure the request includes:
  userId (already used in this route)

- Do NOT modify backend auth logic.
- Do NOT remove requireAdmin from admin route.

----------------------------------------
2. FIX INVENTORY PUSH SEARCH (NOT RETURNING RESULTS)
----------------------------------------

Problem:
Inventory push search is broken due to wrong API endpoint.

Fix:
- Update inventory push modal search to use:
  /api/agent/bigcommerce/search

- Ensure it supports:
  - SKU search
  - UPC search
  - Keyword search

- Handle response types:
  a) resultType: "variant" → single variant result
  b) resultType: "products" → list of mother products

- Maintain current working behavior:
  - Clicking product expands variants
  - Selecting variant allows inventory push

----------------------------------------
3. FIX MAX PURCHASE QUANTITY DETECTION (NOT WORKING)
----------------------------------------

Problem:
Even after switching API, max_purchase_quantity is NOT returned in agent route.

Fix:
In backend route:
GET /api/agent/bigcommerce/search

Locate function:
shapeVariant(...)

ADD the following fields:

min_purchase_quantity: v.min_purchase_quantity ?? null,
max_purchase_quantity: v.max_purchase_quantity ?? null,

Ensure this applies to:
- SKU search result
- UPC search result
- Keyword search result

----------------------------------------
4. FIX CART MAX QUANTITY DETECTION
----------------------------------------

Problem:
Cart does not detect items with max purchase limits.

Fix:
- When adding item to cart:
  Check:
    variant.max_purchase_quantity
    variant.stock_level

- Compute effective max:
    effectiveMax = MIN(stock_level, max_purchase_quantity if exists)

- If max_purchase_quantity exists:
  - Show popup warning:
    "This item has a maximum purchase limit of X"

- DO NOT block adding to cart
- This is only a warning + context

----------------------------------------
5. PRESERVE EXISTING WORKING FEATURES
----------------------------------------

DO NOT BREAK:
- Inventory push functionality (already working before update)
- Variant selection behavior
- Bulk add-to-cart logic
- Order flow

----------------------------------------
6. TEST CASES (MUST PASS)
----------------------------------------

- Searching SKU → returns correct variant
- Searching UPC → returns correct variant
- Searching keyword → returns products
- Inventory push search → returns results (no 403)
- Adding restricted product → shows max quantity warning
- No console 403 errors

----------------------------------------
END