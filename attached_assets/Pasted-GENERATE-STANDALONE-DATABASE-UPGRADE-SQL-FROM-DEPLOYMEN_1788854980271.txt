GENERATE STANDALONE DATABASE UPGRADE SQL
FROM DEPLOYMENT 04a1acb → CURRENT REPLIT SCHEMA

I need you to generate a complete standalone PostgreSQL SQL migration file that I can manually execute in DBeaver.

IMPORTANT:

I am NOT asking you to apply the database changes.

I am NOT asking you to use Replit Publish.

I am NOT asking you to run db:push.

I am NOT asking you to modify the application.

I only want a SQL file that upgrades an existing database whose schema corresponds to deployment 04a1acb so that its schema matches the current/latest Replit application schema.

I will manually execute this SQL in DBeaver against:

1. Staging
2. Live

Staging and Live currently have the same database baseline corresponding to deployment 04a1acb.

==================================================
1. SOURCE OF TRUTH
==================================================

BASELINE:

Deployment / Git commit:

04a1acb

TARGET:

Current/latest Replit application schema.

The project uses:

Drizzle
shared/schema.ts

There is no reliable checked-in SQL migration history covering all changes since 04a1acb.

Therefore, determine the schema difference directly.

==================================================
2. SCHEMA COMPARISON
==================================================

Inspect the database schema represented by deployment 04a1acb.

Compare it against the CURRENT shared/schema.ts.

Determine every schema change required to make the 04a1acb database match the current Replit schema.

Identify:

• new tables
• new columns
• changed columns
• changed data types
• nullable vs NOT NULL changes
• defaults
• primary keys
• foreign keys
• unique constraints
• indexes
• identity/sequence definitions
• enum/type changes
• any other database objects required by the current schema

Do not rely only on line count differences.

Inspect the actual schema definitions.

==================================================
3. GENERATE A REAL POSTGRESQL SQL SCRIPT
==================================================

Generate a standalone PostgreSQL SQL file.

The output must be executable directly in DBeaver against a PostgreSQL database.

Do NOT output:

Drizzle commands
npm commands
db:push commands
Replit Publish instructions
Prisma migrations
JavaScript migration code

I specifically need SQL DDL/DML that DBeaver can execute.

Save the file as:

database-upgrade-04a1acb-to-current.sql

==================================================
4. SAFETY
==================================================

The SQL will eventually be executed against production.

Therefore the script must be NON-DESTRUCTIVE unless a change is absolutely required to match the current schema.

DO NOT:

DROP TABLE

DROP DATABASE

TRUNCATE

DELETE existing customer/order/product/attendance/marketing data

RESET the database

Recreate populated tables unnecessarily

Overwrite existing records

Remove backup tables

Modify backup/legacy tables simply because they are not present in the current schema

The purpose is to bring the schema forward, not clean up the database.

==================================================
5. EXISTING TABLES
==================================================

If a table already exists in the 04a1acb database:

Do not recreate it.

Compare its structure with the target schema.

If columns are missing:

ADD the missing columns.

If indexes are missing:

CREATE the required indexes.

If constraints are missing:

CREATE the required constraints.

If defaults are missing:

ADD the correct defaults.

Use the exact definitions from the current Replit schema.

Do not guess types, defaults, constraints, or relationships.

==================================================
6. NEW TABLES
==================================================

If a table exists in the current Replit schema but did not exist at 04a1acb:

Generate the complete CREATE TABLE statement.

Include:

• columns
• data types
• defaults
• NOT NULL
• primary keys
• foreign keys where appropriate
• unique constraints
• indexes

Create tables in dependency-safe order.

==================================================
7. EXISTING DATA + NEW NOT NULL COLUMNS
==================================================

Pay special attention to adding NOT NULL columns to existing populated tables.

If the current schema requires:

column_name TYPE NOT NULL DEFAULT value

generate a safe migration using the correct target default.

If there is no safe default and existing rows would violate the constraint:

DO NOT guess.

Instead report the affected column and explain the required backfill.

Do not generate unsafe SQL that would fail halfway through a production migration.

==================================================
8. INDEXES AND CONSTRAINTS
==================================================

Compare all indexes and constraints between the baseline and target.

Do not blindly recreate existing indexes.

Use safe creation patterns where appropriate.

For example:

CREATE INDEX IF NOT EXISTS ...

But do not use IF NOT EXISTS to hide a definition conflict.

If an existing index or constraint has the wrong definition:

REPORT IT EXPLICITLY.

Do not silently replace it.

==================================================
9. FOREIGN KEYS
==================================================

Pay special attention to foreign key dependencies.

Before creating a foreign key, verify:

• referenced table exists
• referenced column exists
• data types are compatible
• existing data will not violate the constraint

If existing data would violate a new foreign key:

STOP that portion of the migration and report the conflict.

Do not delete or modify existing data to make the constraint pass.

==================================================
10. BACKUP TABLES
==================================================

The production database contains backup/legacy tables that are not part of the current Replit schema.

Examples include:

orders_backup
products_backup
settings_backup
users_backup
price_history_cache_backup
price_history_cache_backup2
price_history_cache_05112026_live

Do NOT drop them.

Do NOT modify them.

They are intentionally outside the current application schema.

==================================================
11. KNOWN AREAS TO VERIFY
==================================================

Pay particular attention to schema changes involving:

Attendance

attendance_sessions
attendance_audit_log
attendance_exceptions
attendance_location_checkpoints

Marketing

marketing_audiences
marketing_audience_members
marketing_campaigns
marketing_campaign_recipients
marketing_campaign_events
marketing_campaign_activity
marketing_contacts
marketing_customer_preferences
marketing_suppressions
marketing_automations
marketing_automation_steps
marketing_automation_executions

CRM

crm_audit_log
crm_customer_notes
customer_sales_rep
customer_signup_attempts
customer_signups
customers_mirror

Inventory

inventory_audit_tasks
inventory_push_logs

Reporting

report_export_logs

Notifications

notifications

Also inspect every other table and column changed between 04a1acb and the current schema.

Do not assume the above list is complete.

==================================================
12. IMPORTANT: INVENTORY PUSH LOGS
==================================================

Specifically verify inventory_push_logs.

The current Replit schema may contain columns that are not present in the older production version.

Determine the exact target definition from shared/schema.ts.

Do not assume types/defaults.

Generate the appropriate ALTER TABLE statements if columns are missing.

==================================================
13. LARGE EXISTING TABLES
==================================================

Be particularly careful with:

bc_order_line_items
customer_orders_mirror
customers_mirror
price_history_cache

These tables contain substantial existing data.

Do not recreate them.

Do not copy their data.

Do not perform destructive migrations.

Only make the exact schema changes necessary to reach the current target schema.

==================================================
14. TRANSACTION SAFETY
==================================================

Where appropriate, wrap related schema changes in transactions.

However, consider PostgreSQL operations that cannot safely run inside a transaction.

The final SQL should be suitable for execution in DBeaver.

Clearly comment sections that require special handling.

Use comments to identify major sections.

For example:

-- ============================================
-- ATTENDANCE SCHEMA
-- ============================================

==================================================
15. IDEMPOTENCY
==================================================

Make the migration reasonably safe to run more than once where PostgreSQL supports it.

Examples:

CREATE TABLE IF NOT EXISTS
ADD COLUMN IF NOT EXISTS
CREATE INDEX IF NOT EXISTS

However:

Do not use IF NOT EXISTS to hide a schema definition mismatch.

If an existing object differs from the target definition, report the conflict.

==================================================
16. SEQUENCES / IDENTITY
==================================================

Verify identity/sequence behavior for newly created tables and modified primary keys.

Do not accidentally create duplicate sequence objects.

Use the exact identity strategy represented by the current Replit schema.

==================================================
17. DO NOT MODIFY APPLICATION CODE
==================================================

Do not change:

React code
Express code
API routes
Drizzle schema
UI
Attendance logic
Marketing logic
CRM logic
Inventory logic
Anything unrelated to generating the SQL.

The current Replit schema is the TARGET.

We are only generating the database upgrade script.

==================================================
18. OUTPUT
==================================================

Create:

database-upgrade-04a1acb-to-current.sql

The file must contain the complete SQL required to upgrade the schema from:

04a1acb

to:

CURRENT REPLIT SCHEMA

Then provide a report:

BASELINE:
04a1acb

TARGET:
Current Replit schema

TABLES TO CREATE:
[list]

COLUMNS TO ADD:
[list]

COLUMNS TO MODIFY:
[list]

INDEXES TO CREATE:
[list]

CONSTRAINTS TO CREATE:
[list]

OTHER CHANGES:
[list]

POTENTIAL CONFLICTS:
[list]

UNSAFE CHANGES:
[list]

==================================================
19. VERIFICATION SQL
==================================================

At the end of the SQL file, or in a separate file:

database-upgrade-04a1acb-verification.sql

provide read-only verification queries that I can run in DBeaver after the migration.

The verification should check:

• required tables exist
• required columns exist
• indexes exist
• constraints exist
• no required table is missing
• current schema objects are present

Do not modify data during verification.

==================================================
20. FINAL VALIDATION
==================================================

Before giving me the SQL:

Perform a final comparison between:

04a1acb schema

and

current shared/schema.ts

Make sure the generated SQL accounts for ALL required schema changes.

Do not stop after identifying only the obvious new tables.

The objective is:

DATABASE AT 04a1acb
        ↓
RUN SQL IN DBEAVER
        ↓
DATABASE MATCHES CURRENT REPLIT SCHEMA

The resulting database must support the current Replit application without requiring db:push afterward.

==================================================
21. DO NOT EXECUTE
==================================================

DO NOT execute the generated SQL.

DO NOT run db:push.

DO NOT modify Live.

DO NOT modify Staging.

DO NOT run Replit Publish.

ONLY generate and validate the SQL file.

Stop after the SQL and verification files are generated.