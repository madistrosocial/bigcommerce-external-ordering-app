Fix UI overflow issues, implement clean sync footer, and remove sensitive logs while preserving useful debugging logs.

GOAL

- Restore proper scrolling across the app (no hidden content)
- Replace current sync UI with a clean single-line footer
- Remove sensitive console logs (store hash, tokens)
- Keep useful debugging logs intact
- Do NOT break existing logic or layout structure

---

1. FIX OVERFLOW / SCROLLING (GLOBAL)

Remove ALL global overflow locks.

Search and REMOVE:

html, body {
  overflow: hidden;
}

#root {
  overflow: hidden;
}

Also remove Tailwind classes like:
- overflow-hidden
- h-screen (if paired with overflow-hidden)

---

2. RESTORE NORMAL SCROLL

Ensure global CSS has:

html, body, #root {
  height: 100%;
  overflow-y: auto;
}

---

3. FIX ROOT LAYOUT CONTAINERS

Replace:

<div className="h-screen overflow-hidden">

WITH:

<div className="min-h-screen overflow-y-auto">

Apply to:
- App layout
- Admin pages
- Orders page
- POS wrapper

---

4. POS SAFE SCROLL BEHAVIOR

Inside POS component:

useEffect(() => {
  document.body.classList.add("pos-mode");
  return () => document.body.classList.remove("pos-mode");
}, []);

CSS:

body.pos-mode {
  overscroll-behavior: none;
}

---

5. REMOVE SENSITIVE LOGS ONLY

REMOVE logs that expose:
- storeHash
- accessToken
- full BigCommerce config
- raw API responses containing credentials

Examples to REMOVE:

console.log("BC CONFIG:", ...)
console.log("storeHash:", ...)
console.log("token:", ...)

KEEP useful logs like:

console.log("PRICE HISTORY REQUEST:", ...)
console.log("SYNC REQUEST:", ...)
console.error("ERROR:", ...)

---

6. REMOVE EXISTING SYNC UI

Remove:
- Floating sync buttons
- Inline sync indicators
- Duplicate sync status components

---

7. ADD CLEAN FOOTER SYNC BAR (POS ONLY)

Add fixed footer:

<div className="fixed bottom-0 left-0 w-full border-t bg-white px-3 py-1 text-xs flex items-center justify-between z-50">
  <div className="text-gray-500 truncate">
    {syncStatusText}
  </div>

  <button
    onClick={handleManualSync}
    disabled={isSyncing}
    className="text-blue-600 font-medium text-xs disabled:opacity-50"
  >
    {isSyncing ? "Syncing..." : "Sync"}
  </button>
</div>

---

8. SYNC STATUS TEXT (ONE LINE ONLY)

Use:

"No sync yet"
"Syncing price history..."
"Synced just now"
"Last sync: Xm ago"
"Offline mode"

---

9. PREVENT FOOTER OVERLAP

Add:

padding-bottom: 36px;

to main POS container

---

10. SYNC BEHAVIOR RULES

- Auto sync only on login
- Manual sync via footer button
- Disable button while syncing
- No background auto sync

---

11. DO NOT MODIFY

- Price history logic
- BigCommerce integration
- Database schema
- Checkout flow
- Pricing system

---

EXPECTED RESULT

- All pages scroll properly
- No content is cut off
- Clean single-line sync footer
- No sensitive logs in console
- Debug logs still available
- Works on desktop and tablet

END