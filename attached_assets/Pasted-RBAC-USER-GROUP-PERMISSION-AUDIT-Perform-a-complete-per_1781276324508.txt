RBAC / USER GROUP PERMISSION AUDIT

Perform a complete permission audit of the application.

Current issue:

Several CRM modules and Tools modules exist in the navigation but are missing from the User Group permission matrix.

This creates permission inconsistencies because some pages may exist without corresponding permission controls.

Do NOT modify existing permission logic.

Do NOT modify existing user groups.

Only add missing permission definitions and ensure they are wired into the existing RBAC system.

---

## CRM MODULES

Verify all CRM pages are represented.

Expected CRM permissions:

CRM > Customers

CRM > Reactivation Opportunities

CRM > Customer Notes

CRM > CRM Settings

If any CRM route/page exists that is not represented in permissions, add it.

---

## TOOLS MODULES

Verify all Tools pages are represented.

Current issue:

Tools > Promo SKU Tracker is missing from the permission matrix.

Add:

Tools > Promo SKU Tracker

Also verify:

Tools > BC Product Link

Tools > Product Link Logs

Any other Tools page that currently exists.

---

## PERMISSION AUDIT

Scan application routes and navigation.

Identify every page that appears in:

Sidebar Navigation

Top Navigation

Direct Route Access

Admin Settings

Management Pages

Tools Pages

CRM Pages

Verify each has a corresponding permission definition.

Generate a list of:

Existing permissions

Missing permissions

New permissions added

---

## UI REQUIREMENT

The User Group permission page should always remain synchronized with available modules.

No hardcoded manual list.

Permission list should be generated from the same module registry/configuration used by the navigation system whenever possible.

Goal:

If a new module is added later, it should automatically appear in the permission management page without requiring manual updates in multiple places.

---

## VALIDATION

After implementation verify:

CRM > Customers

CRM > Reactivation Opportunities

CRM > Customer Notes

CRM > CRM Settings

Tools > Promo SKU Tracker

Tools > BC Product Link

Tools > Product Link Logs

all appear in User Groups > Module Permissions.

No existing permissions should be removed.

No existing user group assignments should be modified.

Maintain backward compatibility with all current RBAC functionality.
