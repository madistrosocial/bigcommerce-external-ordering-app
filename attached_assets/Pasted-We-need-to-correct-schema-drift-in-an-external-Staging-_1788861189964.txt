We need to correct schema drift in an external Staging PostgreSQL database.

IMPORTANT:
This Replit environment does NOT have access to the user's Staging or Live database. Do not inspect or assume the Replit development database represents Staging or Live.

Use Replit only to determine the expected schema from shared/schema.ts and the 04a1acb/current Drizzle snapshots.

The actual Staging state is being verified separately through DBeaver.

The following discrepancies have been confirmed by DBeaver against the actual Staging database:

1. inventory_push_logs.user_id is missing the expected foreign key to users.id:
   inventory_push_logs_user_id_users_id_fk

2. customer_signups.bigcommerce_customer_id is missing the expected unique constraint:
   customer_signups_bigcommerce_customer_id_unique

3. promo_free_sku_tracker.sku is missing the expected unique constraint:
   promo_free_sku_tracker_sku_unique

4. roles.name is missing the expected unique constraint:
   roles_name_unique

5. permissions.id is GENERATED BY DEFAULT AS IDENTITY in Staging, while the expected schema is GENERATED ALWAYS AS IDENTITY.

First inspect only the expected definitions in shared/schema.ts and the 04a1acb/current Drizzle snapshots.

Do not inspect or modify the Replit database.
Do not assume anything about Staging beyond the five confirmed discrepancies above.

Determine the exact PostgreSQL DDL needed to restore each expected definition.

For permissions.id, inspect the expected identity definition and determine the safest ALTER TABLE statement to change BY DEFAULT to ALWAYS without changing, deleting, or recreating existing rows.

Do not execute any SQL.
Do not run db:push.
Do not Publish.
Do not modify application code.

Generate a standalone correction SQL file:
database-staging-schema-drift-correction.sql

Include ONLY the five corrections above.

Use CREATE/ALTER constraint statements as appropriate.
Do not include any indexes already corrected.
Do not include any other schema changes.

Stop after generating the correction SQL.