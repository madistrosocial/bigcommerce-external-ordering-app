We are entering Investigation Mode.

DO NOT MODIFY ANY CODE.

Do not edit, create, delete, rename, or refactor any files.

Do not switch to Build mode.

Do not implement any new approach.

Do not make assumptions based on documentation examples.

The current implementation is frozen.

Current State

The current checkout fails with:

BigCommerce sync error:

Store credit GraphQL failed (400):

Cannot query field 'applyStoreCreditToCheckout' on type 'CheckoutMutations'

This proves the mutation currently being used does not exist on this store.

Objective

Determine whether Native BigCommerce Store Credit is architecturally possible from our server-side POS.

Not whether we can create another workaround.

Not another implementation.

I want the definitive answer before touching the code again.

Investigation Requirements

I want you to verify every remaining possible native path.

Do not assume.

Use live schema introspection, official BigCommerce documentation, and live API responses.

For every candidate approach, answer:

Does it exist?
Does it work on this Enterprise store?
What authentication does it require?
Why does it fail (if it fails)?
Include the exact evidence.
Investigate every remaining path
1.

Verify whether applyStoreCreditToCheckout exists.

Do not rely on documentation.

Use live GraphQL schema introspection.

Answer:

Does it exist?
Which type exposes it?
If it doesn't exist, show the evidence.
2.

Verify every Checkout mutation available on this store.

List every mutation under:

CheckoutMutations

Confirm whether any mutation references:

Store Credit
Credit
Balance
Customer Credit
3.

Verify every Storefront REST endpoint related to Store Credit.

Determine whether each endpoint is:

browser only
server supported
requires cookies
supports Customer Access Token
supports Customer Impersonation Token
supports Management API token

Provide evidence.

4.

Verify every Management API endpoint related to Store Credit.

Determine whether:

POST /v3/checkouts/{id}/store-credit

actually exists for this store.

If it returns 404,

determine whether:

endpoint not available
feature disabled
customer has zero balance
checkout type mismatch
unsupported API version

Do not speculate.

5.

Determine whether a Management API cart can ever become visible to Storefront APIs.

Answer with evidence.

6.

Determine whether Customer Login JWT is still required now that the OAuth app is successfully installed.

Test only authentication.

Do NOT continue into checkout.

Simply determine whether:

loginWithCustomerLoginJwt

returns a valid customerAccessToken.

7.

Determine whether a Customer Impersonation Token alone can authenticate any Store Credit endpoint.

Not assumptions.

Evidence only.

8.

Determine whether there is any undocumented or officially supported path used by Native BigCommerce Checkout that we have not yet tested.

Compare our findings against the official implementation.

Final Deliverable

Do NOT implement anything.

Instead produce an investigation report with this format:

Candidate
Exists?
Tested?
Works?
Authentication Required
Evidence
Reason Accepted / Rejected

Include every path investigated.

Decision

At the end, answer only one question:

Can Native BigCommerce Store Credit be implemented from a server-side POS using officially supported APIs on this Enterprise store?

Only answer:

YES
NO
UNKNOWN

If YES:

Describe the architecture.

If NO:

Explain exactly which BigCommerce limitation prevents it.

If UNKNOWN:

List exactly what information is still missing.

Absolutely Do Not
Modify code
Refactor code
Switch to Build mode
Implement another workaround
Replace the current implementation
Introduce another architecture
Guess based on documentation examples

The investigation is complete only when every remaining native path has either been proven or eliminated with evidence.