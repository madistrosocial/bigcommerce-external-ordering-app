Fix RBAC routing so Admin users can access ALL agent pages and remove “separate admin app” behavior.

==================================================
🚨 CRITICAL RULES
=================

* DO NOT refactor existing pages
* DO NOT modify business logic (POS, catalog, inventory, etc.)
* ONLY fix route access logic and login redirect
* Keep current layout (SaaSLayout) as-is

==================================================

1. FIX ProtectedRoute (MAIN ISSUE)
   ==================================================

File: App.tsx

Find:

if (role && currentUser.role !== role) {
return <Redirect to="/dashboard" />;
}

REPLACE WITH:

if (role && currentUser.role !== role && currentUser.role !== "admin") {
return <Redirect to="/dashboard" />;
}

---

RESULT:

* Admin can access ALL agent routes
* Agent still restricted to agent routes
* No more blocking of admin on POS / inventory / catalog

==================================================
2. DO NOT CHANGE AdminRoute
===========================

Keep AdminRoute as-is:

* It should still require:
  hasPermission("admin", "view")

This protects admin pages only.

==================================================
3. FIX LOGIN REDIRECT (IMPORTANT)
=================================

Find login success handler.

REMOVE any logic like:

if (user.role === "admin") {
navigate("/admin");
}

---

REPLACE WITH:

if (isMobile) {
navigate("/catalog");
} else {
navigate("/dashboard");
}

---

RESULT:

* Admin and agent land in SAME app
* No separate admin entry point

==================================================
4. DO NOT USE /admin AS MAIN PAGE
=================================

Keep route:

/admin → AdminDashboard

BUT DO NOT redirect users there automatically.

Admin pages must only be accessed via sidebar.

==================================================
5. KEEP SINGLE LAYOUT (CONFIRM)
===============================

Ensure this remains:

<SaaSLayout>
  <Router />
</SaaSLayout>

DO NOT introduce any AdminLayout or conditional layout.

==================================================
EXPECTED RESULT
===============

✔ Admin can access:

* POS
* Catalog
* Inventory
* Orders
* All agent pages

✔ Admin ALSO sees:

* Settings section (admin pages)

✔ Users:

* Still cannot access admin pages

✔ No more “separate admin system”
✔ One unified SaaS dashboard

==================================================
