Apply the following fixes and improvements. Maintain all existing working functionality and do NOT break pricing, cart, checkout, or caching logic.

---

1. VARIANT POPUP (MOTHER TITLE) – BULK ADD + 2-COLUMN LAYOUT

A. Layout Change
- Convert variant list into a responsive 2-column grid (when screen allows)
- Keep single column on smaller screens

---

B. Quantity Behavior (CRITICAL CHANGE)

- Each variant:
  → Quantity input DEFAULT = 0 (NOT 1)

- User can increment quantities for multiple variants

---

C. Remove Individual Add Buttons

- REMOVE:
  → per-variant "Add – $XX" buttons

---

D. Add Single Global Button

- Add ONE button at bottom:
  → "Add Selected to Cart"

---

E. Bulk Add Logic

On click:
- Iterate through all variants
- For each variant:
  IF quantity > 0:
    → Add to cart

- Each added item must:
  → preserve existing pricing logic
  → preserve price source (custom, history, tier, etc.)
  → preserve inventory validation
  → behave exactly like individually added items

---

F. Edge Rules

- Variants with:
  → quantity = 0 → ignored
  → stock = 0 → cannot increment / disabled
  → exceeding max_purchase → still allowed (handled later in checkout override)

---

2. REVERT PINNED PRODUCTS LAYOUT

- Revert to:
  → Single column
  → Compact row style
  → Small thumbnail left

- Remove:
  → 2-column card layout
  → large banner images

---

3. PUSH INVENTORY SEARCH (FULL CATALOG FIX)

- Replace search logic to match POS main search

Must support:
- Product title
- SKU
- UPC

Source:
- Entire BigCommerce catalog (NOT pinned only)

Behavior:
- Show mother titles
- Expand to variants on click
- Select variant → proceed to quantity input

---

4. PUSH INVENTORY MODAL SIZE

- Set modal width: ~95% viewport
- Height: ~90–95%
- Content scrollable

---

5. INVENTORY PUSH LOGGING (FIX MISSING LOGS)

A. Ensure logs are CREATED on every push

Required fields:
- user_id
- username
- product_id
- variant_id
- sku
- product_name
- variant_name
- previous_inventory
- new_inventory
- quantity_added
- reason
- created_at

---

B. Create new page:

"Manual Inventory Push"

- Add to user dropdown menu

Page:
- Table view
- Sorted newest first

---

6. MAX PURCHASE DETECTION (FIX)

- Ensure max_purchase_quantity is fetched for:
  → scanned products
  → searched products
  → pinned products
  → variant selections

---

On add to cart:
- If exists:
  → show warning:
    "This item has a purchase limit (Max: X). You can override during checkout."

- Do NOT block add (still allow up to inventory)

---

7. GENERAL RULES

- Do NOT break:
  → pricing system
  → price tiers
  → cache system
  → checkout flow
  → existing cart behavior

- No duplicate API calls
- Maintain performance

---

EXPECTED RESULT:

- Variant popup supports fast bulk ordering (multi-select via quantity)
- Cleaner UI with 2-column layout
- One-click bulk add to cart
- Pinned products return to efficient layout
- Push inventory works across full catalog (SKU/UPC supported)
- Inventory push logs are recorded and viewable
- Max purchase limits are correctly detected and surfaced